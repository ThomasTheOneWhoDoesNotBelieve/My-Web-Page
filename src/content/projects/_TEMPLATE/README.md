# New project template

1. Duplicate this entire `_TEMPLATE` folder.
2. Rename the copy, e.g. `kanazawa`.
3. Edit `project.md`.
4. Drop photos into `photos/`.
5. Optional: add `cover.jpg` in the project folder to choose a different cover.
6. Push to GitHub. The Projects page and project URL are generated automatically.
